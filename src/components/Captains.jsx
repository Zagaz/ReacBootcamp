export default function Captains({ title, frase, key }) {
     return (
          <>
               <h3>{title}</h3>
               <p > <i> "{frase}" </i> </p>
               <br />
          </>
     )
} 